// const processRespond = (res, result) => { res.respond(result) }
// const processRespondCreated = (res, result) => { res.respondCreated(null, result) }
// const processRespondUpdated = (res, result) => { res.respondUpdated(null, result) }
// const processRespondDeleted = (res, result) => { res.respondDeleted(null, result) }
// const processFail = (res, error) => { res.fail(error) }
// const processFailNotFound = (res, error) => { res.failNotFound(error) }

// module.exports = {
//     processRespond,
//     processRespondCreated,
//     processRespondUpdated,
//     processRespondDeleted,
//     processFail,
//     processFailNotFound
// }